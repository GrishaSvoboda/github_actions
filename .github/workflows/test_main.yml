name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read
  pages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build your site
        run: |
          # Команды сборки вашего сайта

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
